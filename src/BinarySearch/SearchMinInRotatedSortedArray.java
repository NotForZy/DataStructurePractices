package BinarySearch;

public class SearchMinInRotatedSortedArray {
    public SearchMinInRotatedSortedArray() {
        test();
    }


    // if not a increasing array, pick mid
    // drop the increasing part.
    private int binarySearch(int[] input) {

        return 0;
    }

    private void test() {
        int[] input = { 6, 1, 2, 3, 4, 5 };
        System.out.println("The minimum value in rotated sorted array is: " + binarySearch(input));
    }

}
